################################################################################
# PROJECT: Verifiable Private Voting System with Post-Quantum Security
# PURPOSE: Git ignore file - keeps repo clean while preserving working system
# CAUTION: Only heavy files and generated artifacts are ignored
################################################################################

# ============================================================================
# 1. PYTHON VIRTUAL ENVIRONMENTS (478 MB) - MUST IGNORE
# ============================================================================
# Users will recreate with: pip install -r requirements.txt
crypto_voting_env/
venv/
env/
ENV/
.venv
.env
*.py[cod]
__pycache__/
*.egg-info/
dist/
build/

# ============================================================================
# 2. NODE MODULES (if using Node.js tools) - MUST IGNORE
# ============================================================================
node_modules/
npm-debug.log
yarn-error.log

# ============================================================================
# 3. EXTERNAL DEPENDENCIES (heavy, will be downloaded/built)
# ============================================================================
# Circom compiler and libraries - users will install via npm
circom/
circomlib-2/
liboqs-python/

# ============================================================================
# 4. GENERATED ARTIFACTS & BUILD OUTPUTS - MUST IGNORE
# ============================================================================
# Compiled circuits
circuits/__pycache__/
*.wasm
*.sym
*.r1cs

# Build outputs
build/
dist/
*.o
*.so
*.dll

# ============================================================================
# 5. RUNTIME LOGS & TEMPORARY FILES - MUST IGNORE
# ============================================================================
logs/
*.log
*.pid
*.seed
*.pid.lock

# Temporary test files
.pytest_cache/
.coverage
htmlcov/

# ============================================================================
# 6. CRYPTOGRAPHIC KEYS & SENSITIVE DATA - NEVER PUSH!
# ============================================================================
keys/
*.key
*.pem
*.p12
*.pfx
.env
.env.local
secrets.json
config/hsm_config.json

# ============================================================================
# 7. BENCHMARK RESULTS & PERFORMANCE DATA - CAN REGENERATE
# ============================================================================
# These are heavy and can be regenerated by running tests
tests/logs/
tests/results/
academic_benchmark_results/
*.json (only generated data files - source .json files are kept)
*.png
*.pdf

# BUT KEEP:
# - tests/test_*.py (source test files)
# - tests/benchmark_suite.py (benchmark code)
# - tests/generate_publication_data.py (data generation script)

# ============================================================================
# 8. PLATFORM-SPECIFIC FILES - IGNORE
# ============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ============================================================================
# 9. IDE & EDITOR SPECIFIC FILES - IGNORE
# ============================================================================
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject
.settings/
*.sublime-workspace
*.sublime-project

# ============================================================================
# 10. OS-SPECIFIC FILES - IGNORE
# ============================================================================
.AppleDouble
.LSOverride
.TemporaryItems
.VolumeIcon.icns
.fseventsd
Thumbs.db

# ============================================================================
# 11. SYSTEM CACHE FILES - IGNORE
# ============================================================================
*.cache
.mypy_cache/
.dmypy.json
dmypy.json
.pytest_cache/

# ============================================================================
# 12. DEPENDENCY LOCK FILES (auto-generated) - OPTIONAL
# ============================================================================
# Uncomment if you want lock files tracked:
# poetry.lock
# Pipfile.lock

################################################################################
# EXCEPTIONS: DO NOT IGNORE THESE (needed for repo functionality)
################################################################################

# Keep all source code
!pq/
!zk/
!mpc/
!utils/

# Keep configuration and documentation
!requirements.txt
!setup.py
!setup.cfg
!pyproject.toml
!README.md
!PUBLICATION_READY.md
!FINAL_SUBMISSION_GUIDE.md
!DELIVERABLES_SUMMARY.txt
!validate_paper.md
!*.md

# Keep test source files (not results)
!tests/test_*.py
!tests/benchmark_suite.py
!tests/generate_publication_data.py
!tests/__init__.py

# Keep circuit source (not compiled)
!*.circom
!circuits/*.circom

# Keep example data and configs (non-sensitive)
!example_config.json
!sample_data/

# Keep license and contributing guides
!LICENSE
!CONTRIBUTING.md
!CODE_OF_CONDUCT.md

# Keep CI/CD configuration
!.github/
!.gitlab-ci.yml
!.travis.yml

# Keep version control info
!.gitattributes
!.git/

################################################################################
# SPECIAL CASES
################################################################################

# Keep poseidon_constants.js (it's a reference, not generated)
!poseidon_constants.js

# Keep integrated_voting_system.py (main integration code)
!integrated_voting_system.py
!main.py
!generate_preprint_data.py

# ============================================================================
# CLEANUP NOTES FOR DEVELOPERS
# ============================================================================
# Before pushing to GitHub:
#
# 1. Don't commit virtual environment:
#    $ rm -rf crypto_voting_env/
#    (Users will: pip install -r requirements.txt)
#
# 2. Don't commit external dependencies:
#    $ rm -rf circom/ circomlib-2/ liboqs-python/
#    (Users will install via: npm install, pip install liboqs-python)
#
# 3. Don't commit generated test results:
#    $ rm -rf tests/logs/ tests/results/
#    (Users can regenerate: python3 tests/generate_publication_data.py)
#
# 4. Don't commit logs:
#    $ rm -rf logs/
#
# 5. Clean pycache:
#    $ find . -type d -name __pycache__ -exec rm -rf {} +
#    $ find . -type f -name "*.pyc" -delete
#
# 6. NEVER commit these:
#    - .env files with secrets
#    - Private keys in keys/ directory
#    - HSM configuration with PINs
#
# ============================================================================
